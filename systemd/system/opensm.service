[Unit]
Description=InfiniBand Subnet Manager (OpenSM)
After=network.target
Wants=network.target

[Service]
Type=simple
ExecStart=/usr/sbin/opensm -B -p ${HOSTID}
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target